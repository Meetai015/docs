---
title: "shadcn/ui Components"
description: "Using shadcn/ui component library in Gistly"
icon: "layers"
---

# shadcn/ui Components

## What is shadcn/ui?

**shadcn/ui** is not a component library you install as a dependency. It's a collection of **re-usable components** that you copy and paste into your project.

### Key Features

- **Built on Radix UI** - Accessible, unstyled primitives
- **Styled with Tailwind** - Utility-first CSS
- **Fully customizable** - You own the code
- **TypeScript support** - Fully typed components
- **Themes included** - Light and dark mode support

## Installation

The project already has shadcn/ui configured. To add new components:

```bash
npx shadcn-ui@latest add [component-name]
```

### Examples

```bash
# Add a card component
npx shadcn-ui@latest add card

# Add a table component
npx shadcn-ui@latest add table

# Add multiple components
npx shadcn-ui@latest add accordion alert avatar badge
```

## Available Components

### Form Components

| Component | Usage |
|-----------|-------|
| `button` | Clickable actions |
| `input` | Text input fields |
| `textarea` | Multi-line text input |
| `select` | Dropdown selection |
| `checkbox` | Boolean selections |
| `radio-group` | Single choice options |
| `switch` | Toggle switches |
| `slider` | Range selection |
| `label` | Form labels |

### Data Display

| Component | Usage |
|-----------|-------|
| `table` | Tabular data |
| `card` | Content containers |
| `badge` | Status indicators |
| `avatar` | User images |
| `accordion` | Collapsible content |
| `tabs` | Tabbed navigation |
| `separator` | Visual dividers |
| `skeleton` | Loading placeholders |

### Feedback

| Component | Usage |
|-----------|-------|
| `alert` | Important messages |
| `toast` | Non-intrusive notifications |
| `dialog` | Modal dialogs |
| `alert-dialog` | Confirmation dialogs |
| `progress` | Task progress |
| `spinner` | Loading indicator |

### Navigation

| Component | Usage |
|-----------|-------|
| `dropdown-menu` | Context menus |
| `navigation-menu` | Site navigation |
| `breadcrumb` | Page hierarchy |
| `pagination` | List navigation |
| `tabs` | Tabbed content |

### Overlay

| Component | Usage |
|-----------|-------|
| `dialog` | Modal windows |
| `popover` | Floating content |
| `tooltip` | Helpful hints |
| `hover-card` | Expanded content |

## Customization

### Theming

Modify `src/app/globals.css` to customize colors:

```css
:root {
  --primary: 222.2 47.4% 11.2%;
  --primary-foreground: 210 40% 98%;

  --secondary: 210 40% 96.1%;
  --secondary-foreground: 222.2 47.4% 11.2%;

  --destructive: 0 84.2% 60.2%;
  --destructive-foreground: 210 40% 98%;

  --background: 0 0% 100%;
  --foreground: 222.2 84% 4.9%;

  --border: 214.3 31.8% 91.4%;
  --input: 214.3 31.8% 91.4%;
  --ring: 222.2 84% 4.9%;

  --radius: 0.5rem;
}
```

### Component Variants

Components use **Class Variance Authority (CVA)** for variant management:

```tsx
import { cva, type VariantProps } from 'class-variance-authority';

const buttonVariants = cva(
  'inline-flex items-center justify-center rounded-md font-medium',
  {
    variants: {
      variant: {
        default: 'bg-primary text-primary-foreground hover:bg-primary/90',
        destructive: 'bg-destructive text-destructive-foreground',
        outline: 'border border-input bg-background hover:bg-accent',
      },
      size: {
        default: 'h-10 px-4 py-2',
        sm: 'h-9 px-3',
        lg: 'h-11 px-8',
      },
    },
    defaultVariants: {
      variant: 'default',
      size: 'default',
    },
  }
);
```

### Extending Components

Modify any component directly in your project:

```tsx
// src/components/ui/button.tsx

const Button = React.forwardRef<HTMLButtonElement, ButtonProps>(
  ({ className, variant, size, ...props }, ref) => {
    return (
      <button
        className={cn(buttonVariants({ variant, size, className }))}
        ref={ref}
        {...props}
      />
    );
  }
);
```

## Common Patterns

### Form with Validation

```tsx
import { useForm } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import * as z from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import {
  Form,
  FormControl,
  FormField,
  FormItem,
  FormLabel,
  FormMessage,
} from '@/components/ui/form';

const schema = z.object({
  name: z.string().min(2),
  email: z.string().email(),
});

export function MyForm() {
  const form = useForm({
    resolver: zodResolver(schema),
  });

  return (
    <Form {...form}>
      <form onSubmit={form.handleSubmit((data) => console.log(data))}>
        <FormField
          control={form.control}
          name="name"
          render={({ field }) => (
            <FormItem>
              <FormLabel>Name</FormLabel>
              <FormControl>
                <Input {...field} />
              </FormControl>
              <FormMessage />
            </FormItem>
          )}
        />
        <Button type="submit">Submit</Button>
      </form>
    </Form>
  );
}
```

### Dialog with Actions

```tsx
import {
  Dialog,
  DialogContent,
  DialogDescription,
  DialogFooter,
  DialogHeader,
  DialogTitle,
  DialogTrigger,
} from '@/components/ui/dialog';
import { Button } from '@/components/ui/button';

export function ConfirmDialog() {
  return (
    <Dialog>
      <DialogTrigger asChild>
        <Button variant="destructive">Delete</Button>
      </DialogTrigger>
      <DialogContent>
        <DialogHeader>
          <DialogTitle>Are you sure?</DialogTitle>
          <DialogDescription>
            This action cannot be undone.
          </DialogDescription>
        </DialogHeader>
        <DialogFooter>
          <Button variant="outline">Cancel</Button>
          <Button variant="destructive">Delete</Button>
        </DialogFooter>
      </DialogContent>
    </Dialog>
  );
}
```

## Best Practices

1. **Copy, don't install** - Components are copied into your project
2. **Customize freely** - Make changes to suit your needs
3. **Maintain consistency** - Use existing variants when possible
4. **Accessibility first** - Radix UI primitives handle ARIA attributes
5. **Dark mode support** - Use CSS variables for theming

## Resources

- [shadcn/ui Documentation](https://ui.shadcn.com)
- [Radix UI Primitives](https://www.radix-ui.com)
- [Tailwind CSS](https://tailwindcss.com)

## Next Steps

- Learn about [Styling](/frontend/styling)
- Understand [State Management](/frontend/state-management)
- Set up [API Integration](/frontend/api-integration)
